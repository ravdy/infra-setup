apiVersion: v1
kind: Pod
metadata:
  name: network-multitool-extra
  labels:
    purpose: demonstrate-command
spec:
  containers:
    - name: network-multitool
      image: wbitt/network-multitool:alpine-extra
  restartPolicy: OnFailure
